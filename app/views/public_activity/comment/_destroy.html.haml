%p.card-text
  = activity.owner.profile.full_name if activity.owner
  removed a comment on the post titled
  = activity.parameters[:post] ? [activity.parameters[:post][:heading], ' (Removed Post)'].join : 'Removed Post'
